import React from 'react';

export const BlogContext = React.createContext('default');

// export const BlogProvider = ({children}) => {
//     return <BlogContext.Provider value={'data'}>
//         {children}
//     </BlogContext.Provider>
// };